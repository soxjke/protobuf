### Before add to your project as a CocoaPod!

Ensure that protoc compiler is installed!

brew install automake

brew install libtool

brew instal protobuf


### Adding to your project as a CocoaPod

In your Podfile

pod 'AppspectorProtobuf'